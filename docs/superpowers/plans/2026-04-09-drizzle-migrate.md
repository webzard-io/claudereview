# Drizzle Migrate 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将数据库管理从手写 SQL bootstrap 切换到 Drizzle ORM 的 migrate() 系统

**Architecture:** 分 4 个 Phase 渐进实施。Phase 1 补齐现有库漂移，Phase 2 生成 baseline migration，Phase 3 用 migrate() 替换手写 SQL 并精简 Dockerfile，Phase 4 更新文档。

**Tech Stack:** Drizzle ORM, drizzle-kit, Bun SQLite, Docker

**Spec:** `docs/superpowers/specs/2026-04-09-drizzle-migrate-design.md`

---

### Task 1: Phase 1 — 补齐 raw_json 列漂移

**Files:**
- Modify: `src/db/index.ts:84-104` (ALTER TABLE migration block)

- [ ] **Step 1: 在 ALTER TABLE 块末尾补上 raw_json 迁移**

在 `src/db/index.ts:104`（`view_count` 检查之后）追加：

```typescript
if (!existingSessionsColumns.has('raw_json')) {
  sqlite.exec('ALTER TABLE sessions ADD COLUMN raw_json TEXT;');
}
```

- [ ] **Step 2: 验证服务能正常启动**

Run: `bun run start`（启动后 Ctrl+C 退出）
Expected: 无报错，数据库文件中 sessions 表包含 raw_json 列

- [ ] **Step 3: 验证列已补齐**

Run: `sqlite3 ./data/claudereview.db "PRAGMA table_info(sessions);" | grep raw_json`
Expected: 输出包含 `raw_json` 行

---

### Task 2: Phase 2a — 确认 .gitignore 中的 drizzle/ 忽略规则已移除

**Files:**
- Modify: `.gitignore`（如规则仍存在则移除）

- [ ] **Step 1: 确认 drizzle/ 忽略规则已移除**

检查 `.gitignore` 中是否仍有 `drizzle/` 行。如果存在，删除 `# Drizzle migrations` 和 `drizzle/` 两行。如果已不存在（在 brainstorming 阶段已移除），跳过此步。

- [ ] **Step 2: 验证 drizzle/ 不再被忽略**

Run: `git check-ignore drizzle/`
Expected: 无输出（表示不再被忽略）

---

### Task 3: Phase 2b — 生成 baseline migration 并编辑为 IF NOT EXISTS

**Files:**
- Create: `drizzle/XXXX_*.sql` (generated by drizzle-kit)
- Create: `drizzle/meta/_journal.json` (generated by drizzle-kit)
- Create: `drizzle/meta/XXXX_snapshot.json` (generated by drizzle-kit)

- [ ] **Step 1: 生成 baseline migration**

Run: `bun run db:generate`
Expected: `drizzle/` 目录下生成 SQL 文件和 `meta/` 子目录

- [ ] **Step 2: 查看生成的 SQL 文件**

Run: `cat drizzle/*.sql`
Expected: 包含 4 个 `CREATE TABLE` 语句和 6 个 `CREATE INDEX` 语句

- [ ] **Step 3: 编辑 SQL — 将 CREATE TABLE 改为 CREATE TABLE IF NOT EXISTS**

对生成的 SQL 文件执行全局替换：
- `CREATE TABLE` → `CREATE TABLE IF NOT EXISTS`
- `CREATE INDEX` → `CREATE INDEX IF NOT EXISTS`
- `CREATE UNIQUE INDEX` → `CREATE UNIQUE INDEX IF NOT EXISTS`（如有）

- [ ] **Step 4: 验证 meta/_journal.json 存在**

Run: `cat drizzle/meta/_journal.json`
Expected: JSON 文件包含 `entries` 数组，每个 entry 有 `idx`, `when`, `tag`, `breakpoints`

- [ ] **Step 5: 验证编辑后的 SQL 语法正确**

Run: `cat drizzle/*.sql | sqlite3 :memory:`
Expected: 无报错

---

### Task 4: Phase 3a — 重写 src/db/index.ts，用 migrate() 替换手写 SQL bootstrap，并保留兼容补列桥接

**Files:**
- Modify: `src/db/index.ts` (replace table bootstrap, keep legacy column bridge)

- [ ] **Step 1: 重写 src/db/index.ts**

将文件内容替换为：

```typescript
import { drizzle } from 'drizzle-orm/bun-sqlite';
import { Database } from 'bun:sqlite';
import { join, dirname } from 'node:path';
import { mkdirSync, existsSync } from 'node:fs';
import { migrate } from 'drizzle-orm/bun-sqlite/migrator';
import * as schema from './schema.ts';

// Get database path from environment, default to ./data/claudereview.db
const DATABASE_PATH = process.env.DATABASE_PATH || './data/claudereview.db';

// Ensure data directory exists
const dbDir = dirname(DATABASE_PATH);
if (!existsSync(dbDir)) {
  mkdirSync(dbDir, { recursive: true });
}

// Create SQLite connection using Bun's native SQLite
const sqlite = new Database(DATABASE_PATH);

// Enable WAL mode for better concurrent read performance
sqlite.exec('PRAGMA journal_mode = WAL;');

// Compatibility bridge for databases created before drizzle migrations
// ... ALTER TABLE logic for legacy sessions columns ...

// Create drizzle instance
export const db = drizzle(sqlite, { schema });

// Resolve migrations folder relative to this file, not process.cwd()
const migrationsFolder = decodeURIComponent(new URL('../../drizzle', import.meta.url).pathname);
migrate(db, { migrationsFolder });

// Schema assertion: fail fast if compatibility bridge did not align the schema
const cols = sqlite.query('PRAGMA table_info(sessions)').all() as Array<{ name: string }>;
const colNames = new Set(cols.map(c => c.name));
const required = ['message_count', 'tool_count', 'duration_seconds', 'salt', 'owner_key', 'raw_json', 'view_count'];
const missing = required.filter(c => !colNames.has(c));
if (missing.length > 0) {
  throw new Error(
    `Schema assertion failed: sessions table is missing columns: ${missing.join(', ')}. ` +
    'The compatibility bridge did not fully align this database.'
  );
}

// Re-export schema
export * from './schema.ts';
```

- [ ] **Step 2: 验证服务能正常启动（使用已有数据库）**

Run: `bun run start`（启动后 Ctrl+C 退出）
Expected: 无报错，`__drizzle_migrations` 表被自动创建

- [ ] **Step 3: 验证 migrate 创建了 journal 表**

Run: `sqlite3 ./data/claudereview.db ".tables" | grep __drizzle`
Expected: 输出包含 `__drizzle_migrations`

- [ ] **Step 4: 验证全新数据库也能正常初始化**

Run: `DATABASE_PATH=./data/test-fresh.db bun run start`（启动后 Ctrl+C）
Expected: 无报错，全新数据库包含所有 4 张表

- [ ] **Step 5: 清理测试数据库**

Run: `rm -f ./data/test-fresh.db`

---

### Task 5: Phase 3b — 精简 Dockerfile

**Files:**
- Modify: `Dockerfile:22-54` (entire production stage)

- [ ] **Step 1: 重写 Dockerfile 生产阶段**

将 Dockerfile 的 production stage（从 `FROM ${BUN_IMAGE} AS production` 开始）替换为：

```dockerfile
# Production image
FROM ${BUN_IMAGE} AS production

WORKDIR /app

# Copy from builder (no Node.js or drizzle-kit needed at runtime)
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/src ./src
COPY --from=builder /app/package.json ./
COPY --from=builder /app/drizzle ./drizzle

# Create data directory for SQLite
RUN mkdir -p /app/data

# Set environment
ENV NODE_ENV=production
ENV PORT=3000
ENV DATABASE_PATH=/app/data/claudereview.db

# Expose port and data volume
EXPOSE 3000
VOLUME ["/app/data"]

# Run the server
CMD ["bun", "run", "start"]
```

移除的内容：
- `RUN apt-get ... nodejs ...`（L26-27）— 不再需要 Node.js
- `COPY ... drizzle.config.ts`（L33）— 运行时不需要
- `COPY ... /drizzle-tools/node_modules`（L36）— drizzle-kit 工具链
- `RUN printf ... db-push ...`（L39）— db-push 包装脚本

新增的内容：
- `COPY --from=builder /app/drizzle ./drizzle` — migration 文件

- [ ] **Step 2: 验证 Docker 构建成功**

Run: `docker build -t claudereview-test .`
Expected: 构建成功，无报错

- [ ] **Step 3: 验证容器能正常启动**

Run: `docker run --rm -p 3001:3000 claudereview-test &` then `curl http://localhost:3001/health` then `docker stop $(docker ps -q --filter ancestor=claudereview-test)`
Expected: `{"status":"ok"}`

---

### Task 6: Phase 3c — 新增 db:migrate 脚本

**Files:**
- Modify: `package.json` (scripts section)

- [ ] **Step 1: 在 package.json scripts 中新增 db:migrate**

在 `"db:studio"` 之后添加：

```json
"db:migrate": "bun run src/db/index.ts"
```

注意：spec 中写的是 `"bun run src/db/migrate.ts"`（独立脚本），这里直接用 `src/db/index.ts` 因为 import 该模块即触发 `migrate()` 副作用，无需额外文件。

- [ ] **Step 2: 验证脚本可执行**

Run: `bun run db:migrate`
Expected: 无报错退出（migration 已执行过，为空操作）

---

### Task 7: Phase 4 — 更新文档

**Files:**
- Modify: `README.md:151-153`
- Modify: `CLAUDE.md` (Commands section, local-only, gitignored)

- [ ] **Step 1: 更新 README.md 数据库设置章节**

将 `README.md` 中的：

```markdown
## Database Setup

The database schema is created on startup by `src/db/index.ts`.
```

替换为：

```markdown
## Database Setup

Database migrations run automatically on server startup via `drizzle-orm/migrator`. To add schema changes:

1. Edit `src/db/schema.ts`
2. Run `bun run db:generate` to create a migration
3. Commit the `drizzle/` directory (includes `meta/_journal.json`)
4. Deploy — migrations execute on startup

For local development, `bun run db:push` can be used for fast iteration without generating migration files.
```

- [ ] **Step 2: 验证 README 格式正确**

Run: `head -170 README.md | tail -20`
Expected: 显示更新后的 Database Setup 章节

- [ ] **Step 3: 更新 CLAUDE.md Commands 节**

在 CLAUDE.md 的 Commands 代码块中，`bun run db:studio` 之后追加：

```bash
bun run db:migrate   # Run pending migrations manually
```

注意：CLAUDE.md 被 `.gitignore` 忽略，此变更仅影响本地开发环境，不会提交到 git。
